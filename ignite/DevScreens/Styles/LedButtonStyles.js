import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  contentContainer: {
    borderRadius: 15,
    shadowOffset: { width: 0, height: 0 },
    shadowRadius: 11,
    shadowOpacity: 1,
  },
});
